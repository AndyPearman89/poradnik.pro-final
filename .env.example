# Local development stack — copy to .env and adjust as needed.
# Never commit .env with real secrets.

# MySQL / WordPress database credentials
DB_NAME=wordpress
DB_USER=wordpress
DB_PASSWORD=wordpress
DB_ROOT_PASSWORD=rootpassword

# WordPress container port on host
WP_PORT=8080

# Enable WP_DEBUG inside container (0 or 1)
WORDPRESS_DEBUG=0
